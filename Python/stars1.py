# Create a function called  draw_stars() that takes a list of numbers and prints out  *

x = [4, 6, 1, 3, 5, 7, 25]
def draw_stars(x):
    for
    # for i in range(len(x)):
    #
    #     x[i]
    #     i = i + 1
draw_stars(x)
